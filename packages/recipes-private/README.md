# recipes-private (Private Overlay Example)

This package illustrates where private recipe bundles can live outside the public core runtime.

Public distributions should keep generic recipes in public packages and place business-specific recipes in private overlays.
